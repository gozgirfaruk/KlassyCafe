@model List<ResultProductPreviewDto>
<section class="section" id="menu">
    <div class="container">
        <div class="row">
            <div class="col-lg-4">
                <div class="section-heading">
                    <h6>Sizin İçin Seçtiklerimiz</h6>
                    <h2>Ve daha nice lezzetleri tatmak için hemen rezervasyon oluşturun.</h2>
                </div>
            </div>
        </div>
    </div>
    <div class="menu-item-carousel">
        <div class="col-lg-12">
            <div class="owl-menu-item owl-carousel">
                @foreach(var item in Model)
                {
                     <div class="card" style="width: 18rem;">
                        <div class="price"><h6>@item.Price.ToString("c")</h6></div>
                <img src="@item.ImageUrl" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <p class="card-text">@item.Description</p>
                </div>
            </div>
                }
               
        </div>
    </div>
    </div>

    @*  <div class="menu-item-carousel">
    <div class="col-lg-12">
    <div class="owl-menu-item owl-carousel">
    @foreach(var item in Model)
    {
    <div class="item">
    <div class='card card1'>
    <div class="price"><h6>@item.Price.ToString("c")</h6></div>
    <div class='info'>
    <h1 class='title'>@item.Title</h1>
    <p class='description'>@item.Description</p>
    <div class="main-text-button">
    <div class="scroll-to-section"><a href="#reservation">Rezervasyon Oluştur<i class="fa fa-angle-down"></i></a></div>
    </div>
    </div>
    </div>
    </div>
    }

    </div>
    </div>
    </div> *@
</section>

